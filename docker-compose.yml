version: '3.8'

services:
  app:
    build: .
    ports:
      - "8888:8888"
    environment:
      - ENV_VAR=value
    volumes:
      - .:/app
    command: ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8888"]
